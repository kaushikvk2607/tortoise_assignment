# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# Local MongoDB
MONGODB_URI=mongodb://localhost:27017/tortoise_supplier

# MongoDB Atlas (Uncomment to use cloud database)
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/tortoise_supplier?retryWrites=true&w=majority

# CORS Configuration (Frontend URL)
FRONTEND_URL=http://localhost:5173

# JWT Secret (if you plan to add authentication later)
# JWT_SECRET=your_jwt_secret_key_here

# API Rate Limiting (requests per minute)
# RATE_LIMIT=100
